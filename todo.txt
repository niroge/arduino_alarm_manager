TODO:
        - implement the "Connected" check (needs the bluetooth library)
        - correct the alarms vector
        - make the button "Add alarm" actually add an alarm
        - fix the "3 alarms" limitation (with dynamically allocated objects)
